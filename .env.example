# Environment variables for OpenBayanMesh-Edge

# FastAPI Application Settings
# ---------------------------
# Set the environment for the FastAPI application (e.g., development, production)
APP_ENV=development
# Set the default API version to be used if not specified in the request
DEFAULT_API_VERSION=v1
# Enable or disable CORS (Cross-Origin Resource Sharing)
CORS_ENABLED=true
# Comma-separated list of allowed origins for CORS (e.g., http://localhost:3000,https://yourdomain.com). Use '*' for all origins.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000 # Change this to your allowed public domains in production
# Log level for the FastAPI application (e.g., INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO
# Enable or disable API rate limiting
RATE_LIMIT_ENABLED=false
# Maximum number of requests allowed per minute per client IP if rate limiting is enabled
RATE_LIMIT_PER_MINUTE=60

# Neo4j Database Settings
# -----------------------
# Neo4j database URI
NEO4J_URI=bolt://neo4j:7687
# Neo4j database username
NEO4J_USERNAME=neo4j
# Neo4j database password (CHANGE THIS IN PRODUCTION)
NEO4J_PASSWORD=password
# Neo4j database name
NEO4J_DATABASE=neo4j

# Cloudflared Tunnel Settings
# ---------------------------
# Cloudflare Tunnel token (REQUIRED for Cloudflared service)
# Obtain this from your Cloudflare Zero Trust dashboard
TUNNEL_TOKEN=your_cloudflare_tunnel_token_here
# Cloudflare Tunnel ID (REQUIRED for Cloudflared service if not using a named tunnel)
TUNNEL_ID=your_cloudflare_tunnel_id_here

# Other Settings
# -------------
# Region or domain information for the deployment
REGION=PH-NCR
# Comma-separated list of data domains supported by this node (e.g., WEATHER,HEALTH,BUDGET,MAPS)
DATA_DOMAINS=WEATHER,HEALTH
# Enable or disable telemetry/monitoring features (opt-in)
TELEMETRY_ENABLED=false